<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | Lex Duvekot - GZ Psycholoog</title>
  <meta name="description" content="{{ description or 'GZ Psycholoog Lex Duvekot in Haarlem. Sportpsychologie, gezondheid en werk. Geen wachtlijst, direct intake.' }}">
  
  {# Original CSS - copied from WordPress mirror #}
  <link rel="stylesheet" href="/assets/css/style.css">
  
  {# Favicon #}
  <link rel="icon" href="/favicon.ico">
  
  {# Open Graph #}
  <meta property="og:title" content="{{ title }} | Lex Duvekot">
  <meta property="og:description" content="{{ description or '' }}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="{{ page.url | url }}">
  {% if featuredImage %}
  <meta property="og:image" content="{{ featuredImage }}">
  {% endif %}
  
  {# Additional head content from pages #}
  {% block head %}{% endblock %}
</head>
<body class="{{ bodyClass or '' }}">
  
  {% include "partials/header.njk" %}
  
  <main id="main-content">
    {% block content %}
      {{ content | safe }}
    {% endblock %}
  </main>
  
  {% include "partials/footer.njk" %}
  
  {# Original JavaScript - copied from WordPress mirror #}
  <script src="/assets/js/main.js"></script>
  
  {# Additional scripts from pages #}
  {% block scripts %}{% endblock %}
  
</body>
</html>
